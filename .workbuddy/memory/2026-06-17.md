# 2026-06-17 工作日志

## 完成事项
- 对 SJG 数字人文平台进行全栈代码质量审查
  - 审查了 backend/ (43 个 Java 源文件)
  - 审查了 admin-frontend/ (16 个 Vue 源文件)
  - 审查了 display-frontend/ + display-v2/ + display-v3/ (60+ 源文件)
- 生成《技术评估与改进报告》，包含：
  - 7 个严重安全漏洞 (JWT弱密钥、DB密码硬编码、Amap Key泄露等)
  - 4 个架构缺陷 (Controller直连Mapper、Entity作API入参等)
  - 5 处灾难级代码重复 (App.vue 845行完全重复等)
  - 四阶段改进路线图 (安全修复→架构加固→质量提升→团队规范)

## 关键发现
- 项目基础架构合理，但安全隐患严重
- 用户确认：三个展示版本只保留 display-v2
- display-v2 专项审查：6 个运行时 Bug、8 处死代码、5 个安全风险
  - 最严重：G6 v5 API 不兼容（图谱不显示）、CSS 语法错误、Three.js 双重初始化
  - 安全：OSS Bucket URL 硬编码、v-html 无 XSS 净化
  - 主题系统存在双重通道（data-theme + CSS class），应统一到 data-theme
- 团队缺乏统一的编码规范和 Code Review 机制

## P0 Bug 修复 (16:44)
- ✅ Bug 1: App.vue 第 263 行 CSS 语法错误 — `.theme-inkwash class.main-header` → `.theme-inkwash.main-header`
- ✅ Bug 2: PoetList.vue G6 v5 API 不兼容 — 完整迁移到 v5 API:
  - `import * as G6 from '@antv/g6'` → `import { Graph } from '@antv/g6'`
  - `new G6.Graph({ ... }).data(data).render()` → `new Graph({ data, ... }).render()` 
  - `defaultNode`/`defaultEdge` → `node`/`edge` (callbacks for per-node styling)
  - `modes: { default: [...] }` → `behaviors: [...]` (drag-element replaces drag-node)
  - `graphInstance.changeSize()` → `graphInstance.setSize()` (v5 API)
  - 事件处理 `evt.item.getModel().id` → `evt.target?.id`
  - CSS 变量引用改为实际 hex 值（Canvas 渲染不支持 CSS 变量）
  - graphTheme 对象新增 textPrimary/accent/textSecondary/cardBg 包含双主题完整色值

## P1 Bug 修复 + P2 死代码清理 (16:48-17:10)
- ✅ Bug 4: mockFallbackDb.js 图片路径全面修正
  - 11 个景点全部路径从 `_real.jpg` 映射到磁盘实际文件
  - 趵突泉/大明湖/泰山这 3 个有写实版图片的→使用正确文件名
  - 三孔/黄河/光岳楼/苏禄王墓/魏氏庄园/牡丹园这 6 个仅有动漫版→回退用动漫版
  - 岱庙/蒲松龄纪念馆 2 个无任何图片→使用 mount_tai 占位
  - 赵孟頫写实头像不存在→改用 anime 版作为 fallback
- ✅ Bug 5: MapView.vue Three.js 初始化重构
  - 提取共享的 `startThree()` + `loadGeojson()` 函数消除重复逻辑
  - GeoJSON fetch 增加缓存(`cachedGeojson`)避免主题切换时重复请求
  - 移除未使用的 `computed` 导入
- ✅ P2 死代码清理：
  - 删除 HelloWorld.vue (96行 Vite 模板残留)
  - 删除 hero.png/vite.svg/vue.svg 3 个无用资源文件
  - 删除 getFallbackData() (95行从未调用的函数)
  - 删除 mockPoemsList + mockTimeline (被 getFallbackData 独用)
  - 删除 mockCities 未使用导入
  - 删除 variables.css 的 .page-enter-active (从未使用)
  - 删除 inkwash.css 的 .theme-inkwash .seal (从未使用)
  - 删除 AiChatBox.vue 未使用的 onMounted 导入
- mockFallbackDb.js: 413行 → 179行 (-234行)
- ✅ P3 安全:
  - useImage.js: OSS URL 硬编码 → `import.meta.env.VITE_OSS_BUCKET_URL` 环境变量
  - useImage.js: `Array.includes` → `Set.has` (O(1) 性能优化)
  - AiChatBox.vue: `v-html` → `{{ }}` 文本插值, 移除 mock 数据中的 `<strong>` 标签
  - vite.config.js: 移除 `allowedHosts: ['.cpolar.top', '.cpolar.cn']`
  - 新增 .env.example 文件
- ✅ P4 错误处理:
  - api/index.js 重写: 统一错误消息映射(400/401/403/404/500/502/网络/超时)、非200 code 正确抛出业务异常、请求头添加 X-Requested-With
  - PoetDetail/PoemDetail/Timeline/RegionSpots/MapView 5个视图全部添加 try/catch + 错误状态UI
  - Timeline 和 MapView 支持重试机制

## 后端 Spring Boot 审查 (17:04-17:10)
- 审查 backend/ 全部 43 个 Java 源文件 + pom.xml + application.yml
- 发现问题: CRITICAL 3, HIGH 27, MEDIUM 44, LOW 9 (共 83 个)
- Top 3 CRITICAL:
  1. CorsConfig allowCredentials(true) + allowedOriginPattern("*") — CORS 规范冲突
  2. DB 密码默认值 123456
  3. JWT 密钥硬编码
- 批量赋值漏洞: 6 个 Controller 用 @RequestBody Entity 接收参数
- 所有 DTO 无 Bean Validation, 所有 Controller 缺少 @Valid
- Public Controller 全部绕过 Service 层直连 Mapper
- PublicTimelineController 单次请求 25 次 DB 查询 (N+1)
- 全部 Service 零日志、零 @Transactional
- PoetService.delete() 级联删除漏 poem_event 关联 (BUG)

## 数据导入脚本 (17:26-17:36)
- 创建 `数据采集 --分地区/import_data.py` 批量导入脚本
- 解析 8 个 Excel 文件: 198 条记录 → 70 景点 + 130 诗人 + 195 诗词
- 扫描 176 个媒体文件，按目录结构自动分类到 OSS (poets/spots/video/audio)
- 支持 `--sql-only` 模式(仅生成 SQL)和完整模式(上传 OSS + 生成 SQL)
- 生成的 SQL 包含事务包裹、外键级联、自增 ID 修正
- 待确认: 朝代映射边界情况(金代/近现代/当代等)、图片 URL 匹配准确性
